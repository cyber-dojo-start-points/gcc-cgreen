/* The learner is part way through adding a second test file. It is already
   #included from hiker_tests.c, so the half written body is compiled as
   part of hiker_tests.c and stops the build. */

Ensure(hiker, answers_two_digits) {
    assert_that(answer(), is_equal_to(42);
}
